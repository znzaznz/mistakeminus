# 环境变量（含密钥，绝不能进 git）
.env
.env.local
.env.production
.env.*.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
build/

# 临时 / 输出
_demo/
*.log
backfill_preview.txt
import_console.txt
import_log.txt

# 数据库：SQLite 主库与备份均不进 git。
# 题库数据以 data/imports/*.jsonl 为唯一可追踪源，启动时由其重建主库
# （见 backend/app/db.py: seed_from_snapshot）。
*.db
*.db-journal
*.db.bak*
*.bak

# 版权讲义 PDF（付费课程材料，不进 git）
*.pdf

# Tauri 构建产物
**/src-tauri/target/
**/src-tauri/gen/

# TS 增量编译缓存
*.tsbuildinfo
