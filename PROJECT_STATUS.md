# 项目进度与说明

更新时间：2026-06-22

> 本文档是项目的**单一进度入口**：当前做到哪、数据什么状态、怎么维护、下一步做什么。
> 产品设计见 [`PRD.md`](./PRD.md)，环境/启动见 [`README.md`](./README.md)，会话历史见 [`DEVLOG.md`](./DEVLOG.md)。

## 一、项目是什么

MistakeGenie：本地单用户的刷题 / 错题本工具。导入讲义 PDF → 抽成结构化题库 → 按官方考纲知识点组织 → 每日刷题 → 错题自动沉淀、按遗忘曲线复习。免登录、不上云。

科目范围：中级会计三科——**中级会计实务、财务管理、经济法**。

## 二、整体完成进度

| 模块 | 状态 |
|---|---|
| 最小闭环（导入→刷题→错题本→每日任务） | ✅ 已完成 |
| 知识点体系（考点→知识点两层 + 官方能力要求 + 要义） | ✅ 已完成，三科 448 个知识点入库 |
| 题库导入与清洗（PDF 真题 + 自编 + CPA 相邻源） | ✅ 主体完成，可刷池已全绿 |
| 可刷池每知识点覆盖 | ✅ 448/448 知识点已 100% 覆盖（每个 ≥2 题） |
| CPA 相邻题源中级适配筛选 | ⬜ 未开始（1107 道仍在待复核区） |
| App 三科科目筛选 | ⬜ 未做（三科目前混在一起刷） |
| 打包成桌面 App（Tauri） | ⬜ 规划中 |

## 三、当前数据状态

题库总量 **2797 道**，其中可刷池（`needs_review=0`）**1688 道**，结构零缺陷（判断答案均为对/错、题型与答案数一致、选项 key 不重复、答案均在选项内、每题绑定知识点、无空解析）。

**可刷池按来源：**

| 来源 | 数量 | 说明 |
|---|---:|---|
| 环球网校中级真题PDF | 1009 | 逐题修复结构+答案+解析+校对，已转可刷 |
| Claude自编 | 459 | 贴知识点要义编写，过结构闸门入库 |
| PDF阿里质检通过 | 220 | 早期质检通过的真题 |

**三科知识点覆盖：**

| 科目 | 考点 | 知识点 | 覆盖 | 空白 |
|---|---:|---:|---:|---:|
| 中级会计实务 | 71 | 197 | 197 | 0 |
| 财务管理 | 43 | 135 | 135 | 0 |
| 经济法 | 35 | 116 | 116 | 0 |
| 合计 | 149 | 448 | 448 | 0 |

**待复核区（`needs_review=1`）1107 道**，全部是 CPA（注册会计师）相邻题源，难度高于中级，**未经中级适配筛选，暂不可刷**：

| 来源 | 数量 |
|---|---:|
| GitHub题源-IDEAFinBench-CPA会计 | 575 |
| GitHub题源-IDEAFinBench-CPA财务成本管理 | 390 |
| GitHub题源-IDEAFinBench-CPA经济法 | 142 |

## 四、数据与版本库的维护说明

### 数据源与重建（已落实）
**`data/imports/*.jsonl` 是题库数据的唯一可追踪源；`mistakegenie.db`（SQLite）不进 git。**

- 理由：`.db` 是二进制，改一道题整库重写、git 里塞无法 diff 的 blob，仓库越滚越大、history 看不出改了什么；jsonl 是文本，`git diff` 一眼看清改了哪几道题，也适合 review。
- **自动重建**：全新 clone（或删库重来）后，app 启动时（`backend/app/main.py` lifespan → `db.seed_from_snapshot`）自动按 `data/imports/*.jsonl` 逐表重建主库；仅载入当前为空的表，已有数据则跳过，不会覆盖。
- **手动重建**：`cd backend && python -m scripts.import_portable_data`（空表载入）；加 `--force` 先清空 4 张数据表再重灌（jsonl 更新后用）。
- **改完数据要重新导出快照**：`cd backend && python -m scripts.export_portable_data`，再提交 `data/imports/*.jsonl`。这是把本地库变更落进版本库的唯一途径——只改 db 不导出，改动不会进 git。
- **备份**：所有 `mistakegenie.db.bak-*` 收纳在 `backups/`（`.gitignore` 整体忽略）。改库前先 `cp mistakegenie.db backups/mistakegenie.db.bak-<说明>-<时间戳>`。
- **种子源**：自编题的 JSON / 生成脚本放 `_seed/`（`b1.py`–`b6.py` 为补题源，`ch*.json` 为更早的自编源）。

## 五、下一步

1. **CPA 相邻题源中级适配筛选**：逐题判断 1107 道是否契合中级考纲，契合的转可刷、超纲/过难的弃。建议把判定结果写逐题 JSON 存档，支持中断续作。
2. **App 加科目筛选**：避免三科混刷，按科目维度过滤。
3. **可刷池加厚**：当前每知识点至少 2 道，目标骨干题 10–20 道/知识点，按高频考点优先补。
4. **打包桌面 App**（Tauri）。
